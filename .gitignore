# live-build output/cache (regenerated by `lb build`, never commit)
config/chroot/
config/binary/
config/cache/
config/.build/
config/.lock
config/binary.modified_timestamps
config/chroot.files
config/chroot.packages.install
config/chroot.packages.live
config/live-image-*.contents
config/live-image-*.files
config/live-image-*.packages
config/chroot.installed_tmp_pkgs
config/installer_firmware_details.txt
*.iso
*.iso.zsync
*.build.log

# roadmap kept local-only, not on the public repo
ROADMAP.md
Venu-Pacific-Roadmap.html
Venu-Pacific-Roadmap.pdf

# generated by scripts/sync-locales.sh from locales/ — not a source file.
# (Note: only this locales/ subdirectory is generated — other paths under
# usr/share/venu-pacific/, like disaster-info/, are tracked normally.)
config/config/includes.chroot/usr/share/venu-pacific/locales/

# editor / OS cruft
.DS_Store
*.swp
*~
__pycache__/
*.pyc
config/build.log
